# ---- Web framework ----
fastapi~=0.115
uvicorn[standard]~=0.30
pydantic~=2.9
httpx~=0.27

# ---- Audio / DSP ----
numpy~=1.26
scipy~=1.13
soundfile~=0.12
# librosa is handy for analysis; optional for Week 1
# librosa~=0.10

# ---- Core ML stack ----
# GPU / Kaggle (CUDA 12.1 wheels)
torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121

# CPU-only alternative:
# torch==2.3.1

# ---- Coqui TTS ----
TTS~=0.22  # Coqui TTS meta-package

# ---- (Often needed for phonemes; requires system packages) ----
# phonemizer~=3.3
# NOTE: phonemizer needs system deps:
#   Ubuntu: apt-get install -y espeak-ng espeak-ng-data libespeak-ng-dev
# Kaggle image already has many audio libs, but you may still need espeak-ng.

# ---- Dev & tests (optional) ----
# pytest~=8.3
# black~=24.8
# ruff~=0.6
